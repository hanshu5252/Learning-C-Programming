#include<stdio.h>
int main(){
    /*
    int i = 10;
    int *ptr;
    ptr = &i;
    printf("the value of ptr is %u \n",ptr);
    ptr++;   // ptr = ptr+1
    printf("the value of ptr is %u \n",ptr);
    ptr = ptr+2;
    printf("the value of ptr is %u \n",ptr);   */
    char c = 23;
    char *ptr;
    ptr = &c;
    printf("the value of ptr is %u \n",ptr);
    ptr++;   // ptr = ptr+1
    printf("the value of ptr is %u \n",ptr);
    ptr = ptr+2;
    printf("the value of ptr is %u \n",ptr);

}